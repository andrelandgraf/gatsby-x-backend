const buildPurchaseTemplate = require('./purchase');
const buildWelcomeTemplate = require('./welcome');
const buildResetPasswordTemplate = require('./resetPassword');

module.exports = {
  buildPurchaseTemplate,
  buildWelcomeTemplate,
  buildResetPasswordTemplate,
};
